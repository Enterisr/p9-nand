
class EnemyHandler {


   field Array enemies;
   field int enemiesCount;
   field int startScreenLoc;
   static int MAX_ENEMIES;
   constructor EnemyHandler new() {
      let startScreenLoc =100;
      let MAX_ENEMIES = 20;
      let enemiesCount = 0; 
      let enemies = Array.new(MAX_ENEMIES);
      return this;
   }

   method void dispose() {
      do Memory.deAlloc(this);
      return;
   }

    method void spawnEnemies(int num){
        var int i;
        var int memLocationShift;
        var int finalMemLoc;
        var int reqSize;
        var Enemy enemyIt;
        let i = 0;
        let reqSize = enemiesCount + num;

        if(reqSize>MAX_ENEMIES){
            do Sys.error(101);
            return;
        }
        
        let enemiesCount = enemiesCount  +num;
        while(i<num){
            let memLocationShift =finalMemLoc + 2;
            let finalMemLoc = startScreenLoc +memLocationShift;
            let enemyIt = Enemy.new(finalMemLoc); 
            do enemyIt.draw();
            let enemies[i] = enemyIt;
            let i=i+1;
        }

        return;
    }
    method void drawEnemies(){
        
        var int i;
        var Enemy enemyIt;
        let i = 0;
        while(i<enemiesCount){
            let enemyIt = enemies[i]; 
            do enemyIt.move();
            let i=i+1;
        }
        return;
    }

   method void delete() {
      do dispose();
      return;
   }


}
